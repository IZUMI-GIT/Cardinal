// import { NextFunction, Request, Response } from "express";

// export const postBoard = async (req: Request, res: Response, next: NextFunction) => {
    
//     const {title} : {
//         title: string
//     } = req.body;

//     const userId: number = req.userId!;

//     const boardresponse = await postBoard( title, userId );

// }